# Terraform generated files
*.tfstate
*.tfstate.*
*.tfplan
.terraform/
.terraform.lock.hcl

# Lambda generated zip files
lambda/auto_recovery_generated.zip

# Environment-specific files (keep template, ignore actual values if needed)
# terraform.tfvars

# Backup files
*.backup
*.bak
