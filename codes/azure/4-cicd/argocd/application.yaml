# ArgoCD Application for Azure AKS PocketBank
---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: pocketbank
  namespace: argocd
spec:
  description: "PocketBank Application Project for Azure"

  sourceRepos:
    - 'https://github.com/your-org/pocketbank-gitops.git'
    - 'https://github.com/your-org/spring-pocketbank.git'

  destinations:
    - namespace: pocketbank
      server: https://kubernetes.default.svc
    - namespace: pocketbank-staging
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: ''
      kind: Namespace
    - group: 'rbac.authorization.k8s.io'
      kind: ClusterRole
    - group: 'rbac.authorization.k8s.io'
      kind: ClusterRoleBinding

  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'

  orphanedResources:
    warn: true

  roles:
    - name: developer
      policies:
        - p, proj:pocketbank:developer, applications, get, pocketbank/*, allow
        - p, proj:pocketbank:developer, applications, sync, pocketbank/*, allow
      groups:
        - Developers

    - name: admin
      policies:
        - p, proj:pocketbank:admin, applications, *, pocketbank/*, allow
      groups:
        - DevOps-Team

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pocketbank-azure
  namespace: argocd
  labels:
    app: pocketbank
    environment: production
    cloud: azure
  annotations:
    notifications.argoproj.io/subscribe.on-deployed.slack: deployments
    notifications.argoproj.io/subscribe.on-sync-failed.slack: deployments
    notifications.argoproj.io/subscribe.on-deployed.teams: deployments
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: pocketbank

  source:
    repoURL: https://github.com/your-org/pocketbank-gitops.git
    targetRevision: main
    path: overlays/azure-prod

    kustomize:
      images:
        - cloud039/pocketbank-web:latest
        - cloud039/pocketbank-was:latest

  destination:
    server: https://kubernetes.default.svc
    namespace: pocketbank

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      - Validate=true

    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas

  revisionHistoryLimit: 10

---
# Azure 환경용 Kustomize Overlay
# overlays/azure-prod/kustomization.yaml 내용
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pocketbank

resources:
  - ../../base

commonLabels:
  environment: production
  cloud: azure

images:
  - name: pocketbank-web
    newName: cloud039/pocketbank-web
    newTag: latest
  - name: pocketbank-was
    newName: cloud039/pocketbank-was
    newTag: latest

patches:
  - path: patches/deployment-patch.yaml
  - path: patches/ingress-azure-patch.yaml

configMapGenerator:
  - name: pocketbank-config
    behavior: merge
    literals:
      - spring.profiles.active=prod,mysql

secretGenerator:
  - name: pocketbank-db-secret
    type: Opaque
    literals:
      - url=jdbc:mysql://bloberry-mysql.mysql.database.azure.com:3306/pocketbank?useSSL=true

replicas:
  - name: pocketbank
    count: 3

---
# overlays/azure-prod/patches/ingress-azure-patch.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pocketbank
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
    appgw.ingress.kubernetes.io/ssl-redirect: "true"
    appgw.ingress.kubernetes.io/backend-protocol: "http"
    appgw.ingress.kubernetes.io/health-probe-path: "/actuator/health"
    appgw.ingress.kubernetes.io/connection-draining: "true"
    appgw.ingress.kubernetes.io/connection-draining-timeout: "30"
spec:
  tls:
    - hosts:
        - pocketbank.bloberry.cloud
      secretName: pocketbank-tls
  rules:
    - host: pocketbank.bloberry.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pocketbank
                port:
                  number: 80
